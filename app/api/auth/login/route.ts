import {cookies} from "next/headers";
export async function POST(request:Request){const{email,password}=await request.json() as{email?:string;password?:string};const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;if(!url||!key)return Response.json({error:"Login ainda não configurado."},{status:503});if(!email||!password)return Response.json({error:"Informe e-mail e senha."},{status:400});const response=await fetch(`${url}/auth/v1/token?grant_type=password`,{method:"POST",headers:{apikey:key,"Content-Type":"application/json"},body:JSON.stringify({email,password})});const data=await response.json() as{access_token?:string;expires_in?:number;user?:{email?:string}};const allowed=process.env.ADMIN_EMAIL?.trim().toLowerCase();if(!response.ok||!data.access_token||data.user?.email?.toLowerCase()!==allowed)return Response.json({error:"E-mail ou senha inválidos para a administração."},{status:401});(await cookies()).set("arena_admin",data.access_token,{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:data.expires_in||3600});return Response.json({ok:true})}
