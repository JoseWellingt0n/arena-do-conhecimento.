/* eslint-disable react-hooks/static-components, react-hooks/set-state-in-effect, react-hooks/exhaustive-deps, @next/next/no-img-element, @typescript-eslint/no-unused-vars */
"use client";
import { useEffect, useState } from "react";
import { PORTUGUESE_BANK } from "../portuguese-questions";

type Team="Austrália"|"Argentina"|"Inglaterra"|"Marrocos"|"França"|"Espanha"|"África do Sul"|"México"|"Japão"|"Portugal"|"Canadá";
type InstallPromptEvent=Event&{prompt:()=>Promise<void>;userChoice:Promise<{outcome:"accepted"|"dismissed"}>};
type Q={topic:string;text:string;options:string[];answer:number;why:string;difficulty?:"Fácil"|"Médio"|"Difícil"};
type Rank={id:number;name:string;grade:string;team:string;subject?:"math"|"portuguese"|"mixed";score:number;correct:number;createdAt:string;competitionCode?:string};
type TeamRank={team:string;totalScore:number|string|null;totalCorrect:number|string|null;players:number};
type AnswerReview={topic:string;text:string;selectedIndex:number|null;correctIndex:number;options:string[];why:string;timeLeft:number;isCorrect:boolean};
const MATH_BANK:Record<"1º"|"2º"|"3º",Q[]>={
 "1º":[
  {topic:"Matemática básica",text:"Uma cantina vendeu 48 sucos pela manhã e o triplo à tarde. Quantos sucos foram vendidos no total?",options:["144","168","192","216"],answer:2,why:"À tarde foram 3 × 48 = 144 sucos. No total: 48 + 144 = 192."},
  {topic:"Porcentagem",text:"Uma camiseta de R$ 80 recebeu desconto de 15%. Qual é o novo preço?",options:["R$ 65","R$ 68","R$ 70","R$ 72"],answer:1,why:"15% de 80 é 12. Portanto, 80 − 12 = R$ 68."},
  {topic:"Equações",text:"Qual valor de x resolve 3x + 7 = 25?",options:["4","5","6","8"],answer:2,why:"3x = 18, logo x = 6."},
  {topic:"Funções",text:"Se f(x) = 2x − 3, qual é o valor de f(5)?",options:["5","7","10","13"],answer:1,why:"f(5) = 2 × 5 − 3 = 7."},
  {topic:"Sequências",text:"Na sequência 4, 9, 14, 19, … qual é o 7º termo?",options:["29","32","34","39"],answer:2,why:"É uma PA de razão 5: 4, 9, 14, 19, 24, 29, 34."},
  {topic:"Matemática básica",text:"O resultado de 3/4 + 1/6 é:",options:["5/10","7/12","11/12","13/12"],answer:2,why:"O MMC é 12: 9/12 + 2/12 = 11/12."},
  {topic:"Porcentagem",text:"Em uma turma de 40 alunos, 30% faltaram. Quantos alunos compareceram?",options:["12","26","28","30"],answer:2,why:"30% de 40 = 12 faltas. Compareceram 40 − 12 = 28."},
  {topic:"Equações",text:"A soma de um número com seu dobro é 36. Qual é esse número?",options:["9","12","18","24"],answer:1,why:"x + 2x = 36; então 3x = 36 e x = 12."},
  {topic:"Funções",text:"Uma corrida custa R$ 6 de taxa fixa mais R$ 2 por quilômetro. Quanto custa uma viagem de 8 km?",options:["R$ 16","R$ 20","R$ 22","R$ 28"],answer:2,why:"C(8) = 6 + 2 × 8 = R$ 22."},
  {topic:"Sequências",text:"Qual é o próximo termo da sequência 2, 6, 18, 54, …?",options:["108","152","162","216"],answer:2,why:"Cada termo é o anterior multiplicado por 3: 54 × 3 = 162."},
  {topic:"Matemática básica",text:"Qual expressão equivale a 2(3x − 4) + 5?",options:["6x − 3","6x + 1","5x − 8","6x − 13"],answer:0,why:"Aplicando a distributiva: 6x − 8 + 5 = 6x − 3."},
  {topic:"Porcentagem",text:"Um valor aumentou de 200 para 250. Qual foi o aumento percentual?",options:["20%","25%","40%","50%"],answer:1,why:"O aumento foi 50. Como 50/200 = 0,25, o aumento foi de 25%."}
 ],
 "2º":[
  {topic:"Geometria",text:"Um retângulo mede 12 m por 7 m. Qual é sua área?",options:["19 m²","38 m²","72 m²","84 m²"],answer:3,why:"Área do retângulo = 12 × 7 = 84 m²."},
  {topic:"Trigonometria",text:"Em um triângulo retângulo, sen 30° é igual a:",options:["1/2","√2/2","√3/2","1"],answer:0,why:"O valor notável de sen 30° é 1/2."},
  {topic:"Matrizes",text:"Se A = [[2, 1], [3, 4]], qual é a soma dos elementos da diagonal principal?",options:["5","6","7","10"],answer:1,why:"A diagonal principal contém 2 e 4. A soma é 6."},
  {topic:"Sistemas",text:"No sistema x + y = 10 e x − y = 4, qual é o valor de x?",options:["3","6","7","14"],answer:2,why:"Somando as equações: 2x = 14, então x = 7."},
  {topic:"Análise combinatória",text:"Uma lanchonete oferece 3 pães e 4 recheios. Quantos sanduíches com um pão e um recheio podem ser montados?",options:["7","10","12","24"],answer:2,why:"Pelo princípio multiplicativo: 3 × 4 = 12."},
  {topic:"Probabilidade",text:"Ao lançar um dado comum, qual é a probabilidade de sair um número par?",options:["1/6","1/3","1/2","2/3"],answer:2,why:"Há 3 resultados pares entre 6 possíveis: 3/6 = 1/2."},
  {topic:"Geometria",text:"Qual é o comprimento de uma circunferência de raio 5 cm, usando π = 3,14?",options:["15,7 cm","31,4 cm","62,8 cm","78,5 cm"],answer:1,why:"C = 2πr = 2 × 3,14 × 5 = 31,4 cm."},
  {topic:"Trigonometria",text:"Em um triângulo retângulo, a hipotenusa mede 10 e um cateto mede 6. Quanto mede o outro cateto?",options:["4","6","8","16"],answer:2,why:"Pelo teorema de Pitágoras: b² = 100 − 36 = 64; b = 8."},
  {topic:"Matrizes",text:"Qual é o determinante da matriz [[3, 2], [1, 4]]?",options:["8","10","12","14"],answer:1,why:"det = 3 × 4 − 2 × 1 = 10."},
  {topic:"Sistemas",text:"Duas canetas e um caderno custam R$ 18. Uma caneta e um caderno custam R$ 13. Quanto custa uma caneta?",options:["R$ 3","R$ 5","R$ 7","R$ 8"],answer:1,why:"Subtraindo as equações, uma caneta custa R$ 5."},
  {topic:"Análise combinatória",text:"De quantas maneiras 5 estudantes podem ocupar 3 cadeiras em fila?",options:["10","15","60","125"],answer:2,why:"Arranjo de 5 tomados 3 a 3: 5 × 4 × 3 = 60."},
  {topic:"Probabilidade",text:"Uma urna tem 4 bolas azuis e 6 verdes. A probabilidade de retirar uma azul é:",options:["1/5","2/5","3/5","4/5"],answer:1,why:"São 4 azuis em 10 bolas: 4/10 = 2/5."}
 ],
 "3º":[
  {topic:"Geometria analítica",text:"Qual é a distância entre os pontos A(1, 2) e B(4, 6)?",options:["3","4","5","7"],answer:2,why:"d = √[(4−1)² + (6−2)²] = √25 = 5."},
  {topic:"Matemática financeira",text:"R$ 1.000 são aplicados a juros simples de 2% ao mês por 5 meses. Qual é o montante?",options:["R$ 1.020","R$ 1.100","R$ 1.200","R$ 1.500"],answer:1,why:"J = 1000 × 0,02 × 5 = 100. Montante = R$ 1.100."},
  {topic:"Estatística",text:"Qual é a média dos valores 5, 7, 8, 10 e 10?",options:["7","8","9","10"],answer:1,why:"A soma é 40 e 40/5 = 8."},
  {topic:"Polinômios",text:"Qual é o resto da divisão de P(x) = x² − 3x + 5 por x − 2?",options:["2","3","4","5"],answer:1,why:"Pelo teorema do resto, P(2) = 4 − 6 + 5 = 3."},
  {topic:"Revisão ENEM",text:"Uma conta de R$ 240 recebeu desconto de 20% e depois foi dividida igualmente entre 4 pessoas. Quanto cada uma pagou?",options:["R$ 40","R$ 48","R$ 50","R$ 60"],answer:1,why:"Com desconto: 240 × 0,8 = 192. Dividindo por 4: R$ 48."},
  {topic:"Geometria analítica",text:"Qual é o coeficiente angular da reta que passa por (2, 3) e (6, 11)?",options:["1","2","3","4"],answer:1,why:"m = (11−3)/(6−2) = 8/4 = 2."},
  {topic:"Matemática financeira",text:"Uma dívida de R$ 500 aumenta 10% em um mês. Qual é o novo valor?",options:["R$ 510","R$ 540","R$ 550","R$ 600"],answer:2,why:"10% de 500 é 50. Novo valor: R$ 550."},
  {topic:"Estatística",text:"No conjunto 2, 4, 4, 6, 9, qual é a moda?",options:["2","4","5","9"],answer:1,why:"A moda é o valor que mais se repete: 4."},
  {topic:"Polinômios",text:"Quais são as raízes de x² − 5x + 6 = 0?",options:["1 e 6","2 e 3","−2 e −3","3 e 5"],answer:1,why:"x² − 5x + 6 = (x−2)(x−3), portanto as raízes são 2 e 3."},
  {topic:"Revisão ENEM",text:"Um reservatório de 1.200 L está com 35% de sua capacidade. Quantos litros há nele?",options:["360 L","400 L","420 L","480 L"],answer:2,why:"0,35 × 1.200 = 420 litros."},
  {topic:"Estatística",text:"As notas 6, 7, 8 e 9 têm pesos 1, 2, 2 e 1. Qual é a média ponderada?",options:["7","7,5","8","8,5"],answer:1,why:"(6×1 + 7×2 + 8×2 + 9×1)/6 = 45/6 = 7,5."},
  {topic:"Geometria analítica",text:"Qual equação representa a reta de coeficiente angular 3 que passa pela origem?",options:["y = x + 3","y = 3x","y = 3x + 1","x = 3"],answer:1,why:"Uma reta que passa pela origem tem termo independente zero: y = 3x."}
 ]
};
function buildQ(topic:string,text:string,correct:string,distractors:string[],why:string,difficulty:Q["difficulty"],seed:number):Q{const unique=[correct,...distractors.filter((v,i,a)=>v!==correct&&a.indexOf(v)===i)].slice(0,4);while(unique.length<4)unique.push(String(seed+unique.length));const shift=seed%4,options=[...unique.slice(shift),...unique.slice(0,shift)];return{topic,text,options,answer:options.indexOf(correct),why,difficulty}}
const brl=(n:number)=>`R$ ${n.toLocaleString("pt-BR",{minimumFractionDigits:2,maximumFractionDigits:2})}`;
const gcd=(a:number,b:number):number=>b?gcd(b,a%b):a;

const extra1:Q[]=[];
for(let i=1;i<=10;i++){const base=80+i*20,pct=[10,15,20,25,30][i%5],v=base*pct/100;extra1.push(buildQ("Porcentagem",`Quanto é ${pct}% de R$ ${base}?`,brl(v),[brl(v+10),brl(base-v),brl(v*2)],`${pct}/100 × ${base} = ${v}.`,i<4?"Fácil":i<8?"Médio":"Difícil",i))}
for(let i=1;i<=10;i++){const a=2+i%5,x=3+i,b=i+2,c=a*x+b;extra1.push(buildQ("Equações",`Resolva ${a}x + ${b} = ${c}.`,String(x),[String(x+1),String(x-1),String(c-b)],`${a}x = ${c-b}; x = ${x}.`,i<5?"Fácil":"Médio",i+1))}
for(let i=1;i<=9;i++){const m=2+i%4,b=i%3,x=2+i,y=m*x+b;extra1.push(buildQ("Funções",`Se f(x) = ${m}x + ${b}, determine f(${x}).`,String(y),[String(y-m),String(y+b+2),String(m+x+b)],`f(${x}) = ${m} × ${x} + ${b} = ${y}.`,i<4?"Fácil":"Médio",i+2))}
for(let i=1;i<=9;i++){const a=1+i,r=2+i%5,n=5+i%4,v=a+(n-1)*r;extra1.push(buildQ("Sequências",`Uma PA começa em ${a} e tem razão ${r}. Qual é o ${n}º termo?`,String(v),[String(v-r),String(v+r),String(a+n*r)],`aₙ = ${a} + (${n}−1) × ${r} = ${v}.`,i<4?"Médio":"Difícil",i+3))}
MATH_BANK["1º"]=[...MATH_BANK["1º"],...extra1].slice(0,50);

const extra2:Q[]=[];
for(let i=1;i<=8;i++){const w=5+i,h=3+i%5,v=w*h;extra2.push(buildQ("Geometria",`Um retângulo mede ${w} m por ${h} m. Qual é sua área?`,`${v} m²`,[`${2*(w+h)} m²`,`${v+h} m²`,`${w+h} m²`],`Área = ${w} × ${h} = ${v} m².`,i<4?"Fácil":"Médio",i))}
for(let i=1;i<=8;i++){const x=2+i,y=1+i%4,s=x+y,d=x-y;extra2.push(buildQ("Sistemas",`No sistema x + y = ${s} e x − y = ${d}, qual é x?`,String(x),[String(y),String(s),String(d)],`Somando: 2x = ${s+d}; x = ${x}.`,i<4?"Médio":"Difícil",i+1))}
for(let i=1;i<=8;i++){const a=2+i%4,b=1+i%3,c=1+i%2,d=3+i%5,v=a*d-b*c;extra2.push(buildQ("Matrizes",`Calcule o determinante de [[${a}, ${b}], [${c}, ${d}]].`,String(v),[String(a*d+b*c),String(v+2),String(v-2)],`det = ${a} × ${d} − ${b} × ${c} = ${v}.`,i<4?"Médio":"Difícil",i+2))}
for(let i=1;i<=7;i++){const f=2+i,t=f+3+i%4,g=gcd(f,t),v=`${f/g}/${t/g}`;extra2.push(buildQ("Probabilidade",`Uma urna tem ${f} bolas azuis e ${t-f} verdes. Qual a chance de retirar uma azul?`,v,[`${t-f}/${t}`,`${f}/${t+1}`,`1/${t}`],`${f}/${t} = ${v}.`,i<4?"Fácil":"Médio",i+3))}
for(let i=1;i<=7;i++){const a=2+i%4,b=2+(i+1)%4,v=a*b;extra2.push(buildQ("Análise combinatória",`Com ${a} camisetas e ${b} calças, quantos visuais podem ser formados?`,String(v),[String(a+b),String(v-1),String(v+a)],`${a} × ${b} = ${v}.`,i<4?"Fácil":"Médio",i))}
MATH_BANK["2º"]=[...MATH_BANK["2º"],...extra2].slice(0,50);

const extra3:Q[]=[];
for(let i=1;i<=10;i++){const c=500+i*100,r=1+i%5,m=3+i%6,j=c*r*m/100,v=c+j;extra3.push(buildQ("Matemática financeira",`${brl(c)} a juros simples de ${r}% ao mês por ${m} meses resulta em qual montante?`,brl(v),[brl(j),brl(v+c*r/100),brl(c+r*m)],`J = ${brl(j)}; montante = ${brl(v)}.`,i<4?"Médio":"Difícil",i))}
for(let i=1;i<=10;i++){const a=4+i,b=6+i,c=8+i,d=10+i,v=(a+b+c+d)/4;extra3.push(buildQ("Estatística",`Qual é a média de ${a}, ${b}, ${c} e ${d}?`,String(v),[String(v-1),String(v+1),String(a+b)],`A soma é ${a+b+c+d}; a média é ${v}.`,i<5?"Fácil":"Médio",i+1))}
for(let i=1;i<=9;i++){const x=i,y=i+2,x2=i+4,y2=i+6,mx=(x+x2)/2,my=(y+y2)/2;extra3.push(buildQ("Geometria analítica",`Qual é o ponto médio entre A(${x}, ${y}) e B(${x2}, ${y2})?`,`(${mx}, ${my})`,[`(${x+x2}, ${y+y2})`,`(${mx+1}, ${my})`,`(${mx}, ${my+1})`],`M = (${mx}, ${my}).`,i<4?"Médio":"Difícil",i+2))}
for(let i=1;i<=9;i++){const a=1+i%5,b=a+2,s=a+b,p=a*b;extra3.push(buildQ("Polinômios",`Quais são as raízes de x² − ${s}x + ${p} = 0?`,`${a} e ${b}`,[`${-a} e ${-b}`,`${a} e ${s}`,`${p} e ${s}`],`(x−${a})(x−${b}) = 0; raízes ${a} e ${b}.`,i<4?"Médio":"Difícil",i+3))}
MATH_BANK["3º"]=[...MATH_BANK["3º"],...extra3].slice(0,50);

const mixedFor=(year:"1º"|"2º"|"3º")=>[
 ...[...MATH_BANK[year]].sort(()=>Math.random()-.5).slice(0,5),
 ...[...PORTUGUESE_BANK[year]].sort(()=>Math.random()-.5).slice(0,5)
];
const MIXED_BANK:Record<"1º"|"2º"|"3º",Q[]>={
 get "1º"(){return mixedFor("1º")},get "2º"(){return mixedFor("2º")},get "3º"(){return mixedFor("3º")}
};

type TeamConfig={name:Team;grade:string;flag:string;primary:string;secondary:string;dark:boolean};
const teamConfigs:TeamConfig[]=[
 {name:"Austrália",grade:"1º ano A",flag:"/flags/australia.png",primary:"#16835b",secondary:"#2563eb",dark:true},
 {name:"Argentina",grade:"1º ano B",flag:"/flags/argentina.png",primary:"#75b7e8",secondary:"#ffffff",dark:false},
 {name:"Inglaterra",grade:"1º ano C",flag:"/flags/inglaterra.png",primary:"#6b7280",secondary:"#dc143c",dark:true},
 {name:"Marrocos",grade:"1º ano D",flag:"/flags/marrocos.png",primary:"#087443",secondary:"#c9252d",dark:true},
 {name:"França",grade:"2º ano A",flag:"/flags/franca.png",primary:"#082b5b",secondary:"#ef3340",dark:true},
 {name:"Espanha",grade:"2º ano B",flag:"/flags/espanha.png",primary:"#f5c518",secondary:"#aa151b",dark:false},
 {name:"África do Sul",grade:"2º ano C",flag:"/flags/africa-do-sul.png",primary:"#111111",secondary:"#ffb81c",dark:true},
 {name:"México",grade:"2º ano D",flag:"/flags/mexico.png",primary:"#006847",secondary:"#ffffff",dark:true},
 {name:"Japão",grade:"3º ano A",flag:"/flags/japao.png",primary:"#f5f5f5",secondary:"#bc002d",dark:false},
 {name:"Portugal",grade:"3º ano B",flag:"/flags/portugal.png",primary:"#da291c",secondary:"#046a38",dark:true},
 {name:"Canadá",grade:"3º ano C",flag:"/flags/canada.png",primary:"#d8291e",secondary:"#ffffff",dark:true}
];
const grades=teamConfigs.map(t=>t.grade);
const QUESTION_TIME=180;
const formatTime=(seconds:number)=>`${Math.floor(seconds/60)}:${String(seconds%60).padStart(2,"0")}`;
const configFor=(value:string)=>teamConfigs.find(t=>t.name===value)||teamConfigs.find(t=>t.name==="Canadá")!;
const legacyTeam=(value:string,grade?:string)=>["Vermelho","Bandeira","🇨🇦 Canadá"].includes(value)?"Canadá":teamConfigs.some(t=>t.name===value)?value:(teamConfigs.find(t=>t.grade===grade)?.name||value);
const teamLabel=(value:string,grade?:string)=>legacyTeam(value,grade);
const teamColor=(value:string)=>configFor(legacyTeam(value)).primary;
const SHEETS_SCRIPT=`function doPost(e) {
  const output = data => ContentService.createTextOutput(JSON.stringify(data)).setMimeType(ContentService.MimeType.JSON);
  try {
    const payload = JSON.parse(e.postData.contents || "{}");
    if (payload.health) return output({ok: true, health: "ready"});
    const rows = payload.batch || [payload];
    const lock = LockService.getScriptLock();
    lock.waitLock(20000);
    try {
      const file = SpreadsheetApp.getActiveSpreadsheet();
      const sheet = file.getSheetByName("Resultados") || file.insertSheet("Resultados");
      const headers = ["ID", "Nome completo", "Matrícula", "Turma", "Time", "Modalidade", "Rodada", "Tentativa", "Acertos", "Pontos", "Duração", "Data"];
      if (sheet.getLastRow() === 0) sheet.appendRow(headers);
      const ids = sheet.getLastRow() > 1 ? new Set(sheet.getRange(2, 1, sheet.getLastRow() - 1, 1).getDisplayValues().flat()) : new Set();
      const valid = rows.filter(data => data && data.id && data.name && data.grade && data.team);
      const fresh = valid.filter(data => !ids.has(String(data.id)));
      const values = fresh.map(data => [data.id, data.name, data.studentId || "", data.grade, data.team, data.subject || "math", data.competitionCode || "LIVRE", data.attemptNumber || 1, data.correct, data.score, data.duration || 0, data.createdAt || new Date().toISOString()]);
      if (values.length) sheet.getRange(sheet.getLastRow() + 1, 1, values.length, headers.length).setValues(values);
      SpreadsheetApp.flush();
      let emailSent = false;
      if (payload.notifyEmail && /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(payload.notifyEmail)) {
        try {
          const subject = "Arena do Conhecimento — histórico sincronizado";
          const body = "Sincronização concluída com sucesso.\\n\\nNovos resultados: " + values.length + "\\nResultados já existentes: " + (rows.length - values.length) + "\\nTotal processado: " + rows.length + "\\n\\nPlanilha: " + file.getUrl() + "\\n\\nEREM Clóvis Beviláqua";
          MailApp.sendEmail(payload.notifyEmail, subject, body);
          emailSent = true;
        } catch (mailError) {
          emailSent = false;
        }
      }
      return output({ok: true, inserted: values.length, skipped: rows.length - values.length, emailSent: emailSent});
    } finally {
      lock.releaseLock();
    }
  } catch (error) {
    return output({ok: false, error: String(error && error.message || error)});
  }
}`;

export default function Home(){
 const [screen,setScreen]=useState<"start"|"lobby"|"countdown"|"game"|"result">("start"),[name,setName]=useState(""),[grade,setGrade]=useState(""),[team,setTeam]=useState<Team|"">("");
 const [subject,setSubject]=useState<"math"|"portuguese"|"mixed">("math");
 const QUESTION_BANK:Record<"1º"|"2º"|"3º",Q[]>=subject==="math"?MATH_BANK:subject==="portuguese"?PORTUGUESE_BANK:MIXED_BANK;
 const [questions,setQuestions]=useState<Q[]>([]),[index,setIndex]=useState(0),[score,setScore]=useState(0),[correct,setCorrect]=useState(0),[time,setTime]=useState(QUESTION_TIME),[selected,setSelected]=useState<number|null>(null),[answered,setAnswered]=useState(false);
 const [ranking,setRanking]=useState<Rank[]>([]),[teamRanking,setTeamRanking]=useState<TeamRank[]>([]),[rankLoading,setRankLoading]=useState(true),[saved,setSaved]=useState(false),[rankTab,setRankTab]=useState<"players"|"teams">("players"),[rankSubject,setRankSubject]=useState<"all"|"math"|"portuguese"|"mixed">("all"),[clearing,setClearing]=useState(false);
 const [online,setOnline]=useState(true),[installPrompt,setInstallPrompt]=useState<InstallPromptEvent|null>(null),[pendingCount,setPendingCount]=useState(0);
 const [isAdmin,setIsAdmin]=useState(false);
 const [studentId,setStudentId]=useState(""),[competitionCode,setCompetitionCode]=useState(""),[competitionRequired,setCompetitionRequired]=useState(false),[competitionName,setCompetitionName]=useState("Jogo livre"),[startError,setStartError]=useState(""),[starting,setStarting]=useState(false);
 const [sessionToken,setSessionToken]=useState(""),[reviews,setReviews]=useState<AnswerReview[]>([]),[countdown,setCountdown]=useState(3),[gameStartedAt,setGameStartedAt]=useState(0),[saveMessage,setSaveMessage]=useState("");
 const [sheetUrl,setSheetUrl]=useState(""),[sheetEmail,setSheetEmail]=useState(""),[sheetConfigured,setSheetConfigured]=useState(false),[sheetStatus,setSheetStatus]=useState(""),[sheetBusy,setSheetBusy]=useState(false),[showSheetSetup,setShowSheetSetup]=useState(false);
 const [rankPeriod,setRankPeriod]=useState("round"),[fontScale,setFontScale]=useState(1),[highContrast,setHighContrast]=useState(false),[colorBlind,setColorBlind]=useState(false),[reducedMotion,setReducedMotion]=useState(false),[accessOpen,setAccessOpen]=useState(false),[extraTime,setExtraTime]=useState(false);
 const timeLimit=extraTime?240:QUESTION_TIME;
 const q=questions[index],chosen=team?configFor(team):null,accent=chosen?.primary||"#8b5cf6",medal=correct>=9?"🥇":correct>=7?"🥈":correct>=5?"🥉":"🎯",estimatedPosition=(ranking.findIndex(r=>r.score<=score)+1)||ranking.length+1;
 async function loadRanking(period=rankPeriod,mode=rankSubject){setRankLoading(true);try{const r=await fetch(`/api/ranking?period=${period}&subject=${mode}`);const data=await r.json();if(r.ok){setRanking(data.scores);const merged=new Map<string,TeamRank>();for(const item of data.teams as TeamRank[]){const sample=(data.scores as Rank[]).find(s=>s.team===item.team);const key=teamLabel(item.team,sample?.grade),current=merged.get(key);merged.set(key,{team:key,totalScore:Number(current?.totalScore||0)+Number(item.totalScore||0),totalCorrect:Number(current?.totalCorrect||0)+Number(item.totalCorrect||0),players:Number(current?.players||0)+Number(item.players||0)})}setTeamRanking([...merged.values()].sort((a,b)=>Number(b.totalScore)-Number(a.totalScore)))}}finally{setRankLoading(false)}}
 async function clearRanking(){if(!confirm("Tem certeza que deseja excluir todo o histórico do ranking? Essa ação não pode ser desfeita."))return;setClearing(true);try{const r=await fetch("/api/ranking",{method:"DELETE"});if(r.ok){setRanking([]);setTeamRanking([])}}finally{setClearing(false)}}
 async function loadSheetSettings(){try{const r=await fetch("/api/integrations/google-sheets");const data=await r.json();if(r.ok){setSheetUrl(data.url||"");setSheetEmail(data.email||"");setSheetConfigured(Boolean(data.configured))}}catch{}}
 async function saveSheetSettings(){setSheetBusy(true);setSheetStatus("Testando conexão com o Google...");try{const r=await fetch("/api/integrations/google-sheets",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({url:sheetUrl,email:sheetEmail})});const data=await r.json();if(r.ok){setSheetConfigured(Boolean(data.configured));setSheetEmail(data.email||"");setSheetStatus(data.configured?`Conexão salva${data.email?` • relatórios serão enviados para ${data.email}`:""}.`:"Integração removida.")}else setSheetStatus(data.error||"Não foi possível salvar.")}catch{setSheetStatus("Não foi possível testar a conexão.")}finally{setSheetBusy(false)}}
 async function syncSheetHistory(){setSheetBusy(true);setSheetStatus("Enviando histórico...");try{const r=await fetch("/api/integrations/google-sheets",{method:"POST"});const data=await r.json();setSheetStatus(r.ok?`${data.sent} novos resultados enviados${data.skipped?` • ${data.skipped} já estavam na planilha`:""}${data.emailSent?` • resumo enviado para ${data.email}`:data.email?" • planilha atualizada, mas o e-mail não foi disparado":""}.`:(data.error||"Não foi possível sincronizar."))}catch{setSheetStatus("Não foi possível sincronizar. Confira a implantação do Apps Script.")}finally{setSheetBusy(false)}}
 function downloadCsv(){window.location.href="/api/export"}
 function pendingResults(){try{return JSON.parse(localStorage.getItem("pending-scores")||"[]") as object[]}catch{return[]}}
 function queueResult(result:object){const queue=[...pendingResults(),result];localStorage.setItem("pending-scores",JSON.stringify(queue));setPendingCount(queue.length)}
 async function syncResults(){if(!navigator.onLine)return;const queue=pendingResults(),remaining:object[]=[];for(const result of queue){try{const response=await fetch("/api/ranking",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(result)});if(!response.ok)remaining.push(result)}catch{remaining.push(result)}}localStorage.setItem("pending-scores",JSON.stringify(remaining));setPendingCount(remaining.length);if(queue.length!==remaining.length)loadRanking()}
 async function installApp(){if(!installPrompt)return;await installPrompt.prompt();await installPrompt.userChoice;setInstallPrompt(null)}
 const TeamVisual=({value,showName=true,grade:rowGrade}:{value:string;showName?:boolean;grade?:string})=>{const label=teamLabel(value,rowGrade),cfg=configFor(label);return <span className="team-visual"><img className="flag-team" src={cfg.flag} alt={`Bandeira: ${label}`}/>{showName&&<b>{label}</b>}</span>};
 function speakQuestion(){if(!q||!("speechSynthesis" in window))return;window.speechSynthesis.cancel();window.speechSynthesis.speak(new SpeechSynthesisUtterance(`${q.text}. ${q.options.map((o,i)=>`${String.fromCharCode(65+i)}: ${o}`).join(". ")}`))}
 async function start(){if(!name.trim()||!grade||!team||(competitionRequired&&(!studentId.trim()||!competitionCode.trim())))return;setStarting(true);setStartError("");let token="",round="LIVRE";if(navigator.onLine){try{const response=await fetch("/api/game/start",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,grade,team,studentId,competitionCode})});const data=await response.json();if(!response.ok){setStartError(data.error||"Não foi possível iniciar.");setStarting(false);return}token=data.token;round=data.competitionCode;setCompetitionName(data.competitionName||competitionName)}catch{setStartError("Não foi possível validar a competição.");setStarting(false);return}}else if(competitionRequired){setStartError("Conecte-se à internet para participar desta competição.");setStarting(false);return}const year=grade.slice(0,2) as "1º"|"2º"|"3º",bank=QUESTION_BANK[year];let previous:string[]=[];try{previous=JSON.parse(localStorage.getItem(`last-questions-${year}`)||"[]")}catch{}const fresh=bank.filter(item=>!previous.includes(item.text)),pool=fresh.length>=10?fresh:bank;const shuffled=[...pool].sort(()=>Math.random()-.5),take=(level:Q["difficulty"],amount:number)=>shuffled.filter(item=>(item.difficulty||"Médio")===level).slice(0,amount);let picked=[...take("Fácil",3),...take("Médio",4),...take("Difícil",3)];picked=[...picked,...shuffled.filter(item=>!picked.includes(item))].slice(0,10).sort(()=>Math.random()-.5);localStorage.setItem(`last-questions-${year}`,JSON.stringify(picked.map(item=>item.text)));setQuestions(picked);setSessionToken(token);setCompetitionCode(round);setIndex(0);setScore(0);setCorrect(0);setTime(QUESTION_TIME);setAnswered(false);setSelected(null);setSaved(false);setReviews([]);setSaveMessage("");setCountdown(3);setGameStartedAt(Date.now());setScreen("countdown");setStarting(false)}
 function recordAnswer(value:number|null){if(!q)return;const isCorrect=value===q.answer;setReviews(prev=>[...prev,{topic:q.topic,text:q.text,selectedIndex:value,correctIndex:q.answer,options:q.options,why:q.why,timeLeft:time,isCorrect}]);if(isCorrect){setCorrect(v=>v+1);setScore(v=>v+500+Math.round((Math.min(time,QUESTION_TIME)/QUESTION_TIME)*300))}}
 function choose(i:number){if(answered)return;setSelected(i);setAnswered(true);recordAnswer(i)}
 function next(){if(index===questions.length-1){setScreen("result");return}setIndex(v=>v+1);setTime(timeLimit);setSelected(null);setAnswered(false)}
 useEffect(()=>{if(screen!=="countdown")return;if(countdown===3)setTime(timeLimit);if(countdown<=1){const t=setTimeout(()=>setScreen("game"),850);return()=>clearTimeout(t)}const t=setTimeout(()=>setCountdown(v=>v-1),850);return()=>clearTimeout(t)},[countdown,screen]);
 useEffect(()=>{if(screen!=="game"||answered)return;if(time<=0){setAnswered(true);recordAnswer(null);return}const t=setTimeout(()=>setTime(v=>v-1),1000);return()=>clearTimeout(t)},[time,answered,screen]);
 useEffect(()=>{loadRanking();fetch("/api/competition").then(r=>r.json()).then(data=>{setCompetitionRequired(Boolean(data.required));setCompetitionName(data.name||"Jogo livre")}).catch(()=>{});fetch("/api/admin").then(r=>r.json()).then(data=>{const admin=Boolean(data.isAdmin);setIsAdmin(admin);if(admin)loadSheetSettings()}).catch(()=>setIsAdmin(false));setOnline(navigator.onLine);setPendingCount(pendingResults().length);if("serviceWorker" in navigator)navigator.serviceWorker.register("/sw.js");const onOnline=()=>{setOnline(true);syncResults()},onOffline=()=>setOnline(false),onPrompt=(event:Event)=>{event.preventDefault();setInstallPrompt(event as InstallPromptEvent)};window.addEventListener("online",onOnline);window.addEventListener("offline",onOffline);window.addEventListener("beforeinstallprompt",onPrompt);syncResults();return()=>{window.removeEventListener("online",onOnline);window.removeEventListener("offline",onOffline);window.removeEventListener("beforeinstallprompt",onPrompt)}},[]);
 useEffect(()=>{if(screen!=="result"||saved||!team||reviews.length!==10)return;setSaved(true);const result={name,grade,team,studentId,competitionCode,sessionToken,answers:reviews,duration:Math.round((Date.now()-gameStartedAt)/1000)};if(!navigator.onLine){queueResult(result);setSaveMessage("Resultado guardado no aparelho para envio posterior.");return}fetch("/api/ranking",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(result)}).then(async response=>{const data=await response.json();if(!response.ok){setSaveMessage(data.error||"Resultado aguardando novo envio.");if(competitionCode==="LIVRE")queueResult(result)}else{setScore(data.score.score);setCorrect(data.score.correct);setSaveMessage("Resultado validado e salvo com segurança.");loadRanking()}}).catch(()=>{setSaveMessage("Resultado aguardando novo envio.");if(competitionCode==="LIVRE")queueResult(result)})},[screen,saved,team,name,grade,score,correct,reviews,studentId,competitionCode,sessionToken,gameStartedAt]);
 const SubjectPicker=()=> <section className="subject-picker"><div><small>ESCOLHA COMO QUER JOGAR</small><h2>Qual será o desafio?</h2><p>As questões são adaptadas ao ano da sua turma.</p></div><div className="subject-cards"><button className={subject==="math"?"active math":"math"} onClick={()=>setSubject("math")}><i>∑</i><span><b>Matemática</b><small>Raciocínio, cálculos e ENEM</small></span><em>{subject==="math"?"Selecionado":"Escolher"}</em></button><button className={subject==="portuguese"?"active portuguese":"portuguese"} onClick={()=>setSubject("portuguese")}><i>文</i><span><b>Português</b><small>Interpretação, gramática e literatura</small></span><em>{subject==="portuguese"?"Selecionado":"Escolher"}</em></button><button className={subject==="mixed"?"active mixed":"mixed"} onClick={()=>setSubject("mixed")}><i>✦</i><span><b>Desafio aleatório</b><small>5 de Matemática + 5 de Português</small></span><em>{subject==="mixed"?"Selecionado":"Surpreenda-me"}</em></button></div></section>;
 const RankSubjectFilter=()=> <nav className="subject-rank-filter" aria-label="Filtrar ranking por modalidade">{[["all","Geral"],["math","Matemática"],["portuguese","Português"],["mixed","Aleatório"]].map(([value,label])=><button key={value} className={rankSubject===value?"active":""} onClick={()=>{const next=value as typeof rankSubject;setRankSubject(next);loadRanking(rankPeriod,next)}}>{label}</button>)}</nav>;
 const PerformancePanel=()=>{const map=new Map<string,{total:number;correct:number}>();reviews.forEach(r=>{const key=r.topic.replace(/^Português • /,"");const item=map.get(key)||{total:0,correct:0};item.total++;if(r.isCorrect)item.correct++;map.set(key,item)});const items=[...map].map(([topic,value])=>({topic,...value,pct:Math.round(value.correct/value.total*100)})).sort((a,b)=>b.pct-a.pct);return <section className="performance-panel"><div><p className="eyebrow">MAPA DE APRENDIZAGEM</p><h2>Seu desempenho por conteúdo</h2></div><div className="performance-grid">{items.map(item=><article key={item.topic}><header><b>{item.topic}</b><strong>{item.pct}%</strong></header><i><span style={{width:`${item.pct}%`}}/></i><small>{item.correct} de {item.total} corretas • {item.pct>=70?"Você domina este conteúdo":"Vale revisar este assunto"}</small></article>)}</div></section>};
 const Ranking=()=> <section className="ranking"><div className="ranking-title"><div><p className="eyebrow">CLASSIFICAÇÃO GERAL</p><h2>Ranking da competição</h2></div><span>TOP 500</span></div><div className="rank-tools"><div className="rank-tabs"><button className={rankTab==="players"?"active":""} onClick={()=>setRankTab("players")}>Por jogador</button><button className={rankTab==="teams"?"active":""} onClick={()=>setRankTab("teams")}>Por time</button></div>{isAdmin&&<div className="admin-actions"><button className="export-rank" onClick={downloadCsv}>Baixar CSV</button><button className="sheets-rank" onClick={()=>setShowSheetSetup(v=>!v)}>Planilhas {sheetConfigured?"✓":""}</button><button className="clear-rank" onClick={clearRanking} disabled={clearing||ranking.length===0}>{clearing?"Excluindo...":"Excluir histórico"}</button></div>}</div>{isAdmin&&showSheetSetup&&<div className="sheets-setup"><div className="sheets-heading"><div><strong>Google Planilhas</strong><small>{sheetConfigured?"Integração automática ativa":"Configure uma vez para enviar os resultados automaticamente"}</small></div><span className={sheetConfigured?"connected":""}>{sheetConfigured?"Conectado":"Pendente"}</span></div><ol><li>Crie uma Planilha Google e abra <b>Extensões → Apps Script</b>.</li><li>Cole o código abaixo e clique em <b>Implantar → Nova implantação → App da Web</b>.</li><li>Escolha acesso para <b>Qualquer pessoa</b>, copie a URL terminada em <b>/exec</b> e cole no campo.</li></ol><div className="script-box"><pre>{SHEETS_SCRIPT}</pre><button onClick={()=>navigator.clipboard.writeText(SHEETS_SCRIPT)}>Copiar código</button></div><label><span>URL da implantação</span><input value={sheetUrl} onChange={e=>setSheetUrl(e.target.value)} placeholder="https://script.google.com/macros/s/.../exec"/></label><div className="sheet-buttons"><button onClick={saveSheetSettings} disabled={sheetBusy}>{sheetBusy?"Aguarde...":"Salvar integração"}</button><button onClick={syncSheetHistory} disabled={sheetBusy||!sheetConfigured||ranking.length===0}>Enviar histórico agora</button></div>{sheetStatus&&<p>{sheetStatus}</p>}</div>}{rankLoading?<p className="empty">Carregando ranking...</p>:ranking.length===0?<p className="empty">O primeiro campeão pode ser você!</p>:rankTab==="players"?<div className="table-wrap"><table><thead><tr><th>#</th><th>Aluno</th><th>Time</th><th>Acertos</th><th>Pontos</th></tr></thead><tbody>{ranking.slice(0,500).map((r,i)=><tr key={r.id}><td><b className={`place p${i+1}`}>{i+1}</b></td><td><strong>{r.name}</strong><small>{r.grade}</small></td><td><TeamVisual value={r.team} grade={r.grade}/></td><td>{r.correct}/10</td><td><strong>{r.score.toLocaleString("pt-BR")}</strong></td></tr>)}</tbody></table></div>:<div className="team-rank-grid">{teamRanking.map((r,i)=><article key={r.team} style={{"--team-color":teamColor(r.team)} as React.CSSProperties}><b className={`place p${i+1}`}>{i+1}</b><div><strong><TeamVisual value={r.team}/></strong><small>{r.players} participações • {Number(r.totalCorrect).toLocaleString("pt-BR")} acertos</small></div><em>{Number(r.totalScore).toLocaleString("pt-BR")}<small> pontos</small></em></article>)}</div>}</section>;
 return <main className={`app ${chosen?"has-team":""} ${chosen&&!chosen.dark?"light-team":""} ${highContrast?"high-contrast":""} ${colorBlind?"color-blind":""} ${reducedMotion?"reduce-motion":""}`} style={{"--accent":accent,"--accent-2":chosen?.secondary||"#6366f1","--flag-image":chosen?`url(${chosen.flag})`:"none",fontSize:`${fontScale}rem`} as React.CSSProperties}><div className="orb a"/><div className="orb b"/>{chosen&&<div className="country-ambience" aria-hidden="true"><img src={chosen.flag} alt=""/><span>{chosen.name}</span></div>}<div className={`accessibility ${accessOpen?"open":""}`}><button className="access-trigger" aria-expanded={accessOpen} onClick={()=>setAccessOpen(v=>!v)}>♿ <span>Acessibilidade</span></button>{accessOpen&&<section className="access-drawer"><header><div><small>EXPERIÊNCIA INCLUSIVA</small><strong>Acessibilidade</strong></div><button aria-label="Fechar acessibilidade" onClick={()=>setAccessOpen(false)}>×</button></header><label>Tamanho do texto<div><button onClick={()=>setFontScale(v=>Math.max(.9,v-.1))}>A−</button><span>{Math.round(fontScale*100)}%</span><button onClick={()=>setFontScale(v=>Math.min(1.3,v+.1))}>A+</button></div></label><button className={highContrast?"active":""} aria-pressed={highContrast} onClick={()=>setHighContrast(v=>!v)}>◐ Alto contraste <i>{highContrast?"Ativo":"Inativo"}</i></button><button className={colorBlind?"active":""} aria-pressed={colorBlind} onClick={()=>setColorBlind(v=>!v)}>◉ Modo daltônico <i>{colorBlind?"Ativo":"Inativo"}</i></button><button className={reducedMotion?"active":""} aria-pressed={reducedMotion} onClick={()=>setReducedMotion(v=>!v)}>◌ Reduzir animações <i>{reducedMotion?"Ativo":"Inativo"}</i></button>{screen==="game"&&<button onClick={speakQuestion}>🔊 Ouvir a questão</button>}</section>}</div><div className="connection-tools"><span className={online?"online":"offline"}><i/>{online?"Online":"Modo offline"}</span>{pendingCount>0&&<small>{pendingCount} resultado{pendingCount>1?"s":""} aguardando envio</small>}{installPrompt&&<button onClick={installApp}>Instalar jogo</button>}{isAdmin&&<a className="admin-panel-link" href="/admin">Painel administrativo</a>}</div>
 {screen==="start"&&<SubjectPicker/>}
 {screen!=="start"&&<div className={`subject-badge ${subject}`}>{subject==="math"?"∑ Matemática":subject==="portuguese"?"文 Português":"✦ Desafio aleatório"}</div>}
 {screen==="start"&&<div className="start-stack"><section className="card start compact-start"><div className="school-logo"><img src="/logo-erem-clovis-bevilaqua.png" alt="Logo da EREM Clóvis Beviláqua"/></div><p className="school-name">EREM CLÓVIS BEVILÁQUA</p><p className="eyebrow">JOGOS INTERNOS • DESAFIO ENEM</p><h1>Desafio <span>Matemático</span></h1><p className="sub">Escolha sua delegação e represente sua turma.</p><div className={`competition-banner ${competitionRequired?"active":""}`}><b>{competitionRequired?"🏁 Competição ativa":"🎮 Jogo livre"}</b><span>{competitionName}{competitionRequired?" • Digite o código fornecido pelo professor":" • Treine quantas vezes quiser"}</span></div><div className="form"><label className="wide"><span>Nome completo</span><input value={name} onChange={e=>setName(e.target.value)} placeholder="Digite seu nome"/></label>{competitionRequired&&<><label><span>Matrícula ou código do aluno</span><input value={studentId} onChange={e=>setStudentId(e.target.value)} placeholder="Sua identificação"/></label><label><span>Código da competição</span><input value={competitionCode} onChange={e=>setCompetitionCode(e.target.value.toUpperCase())} placeholder="Ex.: MAT2026"/></label></>}<label className="wide"><span>Turma</span><select value={grade} onChange={e=>{const g=e.target.value;setGrade(g);setTeam((teamConfigs.find(t=>t.grade===g)?.name||"") as Team|"")}}><option value="">Selecione</option>{grades.map(g=><option key={g}>{g}</option>)}</select></label><div className="team wide"><span>Delegações por turma</span><div className="team-groups">{["1º","2º","3º"].map(year=><section key={year}><h3>{year} ANO</h3><div>{teamConfigs.filter(t=>t.grade.startsWith(year)).map(t=><button key={t.name} onClick={()=>{setTeam(t.name);setGrade(t.grade)}} className={team===t.name?"on":""} style={{"--team":t.primary,"--detail":t.secondary} as React.CSSProperties}><img className="flag-team" src={t.flag} alt={`Bandeira: ${t.name}`}/><span><b>{t.name}</b><small>{t.grade}</small></span>{team===t.name&&<em>Sua delegação</em>}</button>)}</div></section>)}</div></div></div>{startError&&<p className="start-error">{startError}</p>}<button className="primary" disabled={starting||!name.trim()||!grade||!team||(competitionRequired&&(!studentId.trim()||!competitionCode.trim()))} onClick={start}>{starting?"Validando...":"Iniciar desafio"} <b>→</b></button><aside><span>10 perguntas sem repetição</span><span>1min20s por questão</span><span>Pontuação protegida</span></aside></section><Ranking/></div>}
 {screen==="countdown"&&<section className="countdown-screen"><img src={chosen?.flag} alt=""/><p>Prepare-se, {name.split(" ")[0]}!</p><strong key={countdown}>{countdown}</strong><span>{competitionName} • {grade} • {team}</span></section>}
 {screen==="game"&&q&&<section className="game"><header><div><small>JOGADOR</small><strong>{name}</strong><em>{grade} • Time <TeamVisual value={team}/></em></div><div className="points"><small>PONTUAÇÃO</small><strong>{score.toLocaleString("pt-BR")}</strong></div></header><div className="bar"><i style={{width:`${(index+1)*10}%`}}/></div><div className="meta"><span>QUESTÃO {index+1} <b>DE 10</b></span><div className="timer-card"><span>TEMPO RESTANTE</span><div className={time<=10?"timer danger":"timer"}><strong>{formatTime(time)}</strong><small>min</small></div></div></div><article key={index} className="question-card"><p className="topic-badge">{q.topic} · {q.difficulty||"Médio"}</p><h2>{q.text}</h2></article><div className="answers">{q.options.map((o,i)=>{const s=answered?(i===q.answer?"right":selected===i?"wrong":"muted"):"";return <button className={s} key={o} onClick={()=>choose(i)}><b>{String.fromCharCode(65+i)}</b><span>{o}</span></button>})}</div>{answered&&<div className="feedback"><div><strong>{selected===q.answer?"Mandou bem!":time===0?"Tempo esgotado!":"Quase lá!"}</strong><p>{q.why}</p></div><button onClick={next}>{index===9?"Ver resultado":"Próxima"} →</button></div>}</section>}
 {screen==="result"&&<div className="start-stack"><section className="card result result-pro"><div className="confetti" aria-hidden="true"><i/><i/><i/><i/><i/><i/></div><div className="trophy">{medal}</div><p className="eyebrow">DESAFIO CONCLUÍDO</p><h1>Parabéns, <span>{name.split(" ")[0]}</span>!</h1><p className="sub">{saveMessage||"Validando seu resultado..."}</p><div className="final"><small>PONTUAÇÃO FINAL</small><strong>{score.toLocaleString("pt-BR")}</strong><span>posição estimada: #{estimatedPosition}</span></div><div className="stats"><div><strong>{correct}/10</strong><span>Acertos</span></div><div><strong>{correct*10}%</strong><span>Aproveitamento</span></div><div><strong><TeamVisual value={team}/></strong><span>Time</span></div></div><div className="contribution"><b>+{score.toLocaleString("pt-BR")} pontos</b><span>contribuídos para o time {team}</span></div><button className="primary" onClick={()=>setScreen("start")}>Jogar novamente <b>↻</b></button></section><section className="review-card"><div className="review-title"><div><p className="eyebrow">REVISÃO PERSONALIZADA</p><h2>Aprenda com cada questão</h2></div><span>{reviews.filter(r=>!r.isCorrect).length} para revisar</span></div><div className="review-list">{reviews.map((r,i)=><details key={`${r.text}-${i}`} className={r.isCorrect?"review-ok":"review-wrong"}><summary><b>{r.isCorrect?"✓":"!"}</b><span><small>{r.topic}</small>{i+1}. {r.text}</span><em>{r.isCorrect?"Acertou":"Revisar"}</em></summary><div><p><strong>Sua resposta:</strong> {r.selectedIndex===null?"Não respondeu":r.options[r.selectedIndex]}</p><p><strong>Resposta correta:</strong> {r.options[r.correctIndex]}</p><aside>{r.why}</aside></div></details>)}</div></section><Ranking/></div>}
 {screen==="result"&&<PerformancePanel/>}
 {screen==="result"&&<section className="achievement-board"><div><small>CONQUISTAS DESBLOQUEADAS</small><h2>Sua jornada nesta rodada</h2></div><div>{correct===10&&<span>🏆 <b>Gabaritou</b><small>10 respostas corretas</small></span>}{correct>=8&&<span>⭐ <b>Craque do Conhecimento</b><small>Excelente aproveitamento</small></span>}{reviews.some((r,i)=>i>=2&&reviews.slice(i-2,i+1).every(x=>x.isCorrect))&&<span>🔥 <b>Sequência brilhante</b><small>3 acertos consecutivos</small></span>}{correct<8&&<span>🚀 <b>Missão cumprida</b><small>Desafio finalizado</small></span>}</div></section>}
 {(screen==="start"||screen==="result")&&<nav className="period-filter">{[["round","Rodada"],["week","7 dias"],["month","30 dias"],["general","Geral"]].map(([v,l])=><button className={rankPeriod===v?"active":""} key={v} onClick={()=>{setRankPeriod(v);loadRanking(v)}}>{l}</button>)}</nav>}
 {(screen==="start"||screen==="result")&&<RankSubjectFilter/>}
 <footer>ARENA DO CONHECIMENTO • EREM CLÓVIS BEVILÁQUA • 2026</footer></main>
}
